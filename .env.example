# ============================================
# PUBLIC ENV VARS (Exposed to browser)
# ============================================

# Client calls local SvelteKit API routes (empty base URL = relative paths)
PUBLIC_API_BASE_URL=
PUBLIC_API_AVAILABILITY_PATH=/api/availability
PUBLIC_API_BOOKINGS_PATH=/api/bookings

# ============================================
# PRIVATE ENV VARS (Server-side only)
# ============================================

# External API endpoints - never exposed to browser
# Server proxies all requests to keep this secure
EXTERNAL_API_BASE_URL=https://your.domain.here
EXTERNAL_API_AVAILABILITY_PATH=/api/availability
EXTERNAL_API_MEETINGS_PATH=/api/meetings

# ============================================
# AI API KEYS (for E2E testing)
# ============================================

# At least ONE of these is required for e2e tests to work

# Option 1: Google Gemini (Recommended - free tier available)
# Get your key at: https://makersuite.google.com/app/apikey
GOOGLE_GENERATIVE_AI_API_KEY=

# Option 2: OpenAI (requires paid account)
OPENAI_API_KEY=

# Option 3: Anthropic Claude
ANTHROPIC_API_KEY=

# Optional: For cloud browser testing with Browserbase
# Get your keys from https://browserbase.com/
BROWSERBASE_API_KEY=
BROWSERBASE_PROJECT_ID=

# ============================================
# E2E TESTING CONFIGURATION
# ============================================

# Base URL for E2E tests (defaults to http://localhost:5173 if not set)
# Use this to test against different ports or deployment environments
E2E_BASE_URL=http://localhost:5173
